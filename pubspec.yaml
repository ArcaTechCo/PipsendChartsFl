name: pipsend_charts
description: Advanced financial charting library with built-in technical indicators - Simple, powerful.
version: 1.0.0
homepage: https://pipsend.com/charts#flutter

environment:
  sdk: ">=2.12.0 <4.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ">=0.17.0 <0.21.0"

dev_dependencies:
  flutter_lints:
  flutter_test:
    sdk: flutter
